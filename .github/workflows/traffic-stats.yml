name: Traffic to Badge

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Traffic to Badge
        uses: yi-Xu-0100/traffic-to-badge@v1
        with:
          my_token: ${{ secrets.GHTOKEN }}
